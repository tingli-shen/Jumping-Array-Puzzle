#include <iostream>
#include <math.h>
#include <vector>
#include <string>
using namespace std;
class tree
{
private:
public:
	string num;
	int zero;
	int cul;
	int row;
	void setZero(int x) { zero = x; };
	void setString(string x) { num = x; };
	int zeroSite();

};
